# API Configuration
VITE_API_BASE_URL=http://localhost:3001/api

# Authentication Mode
# Set to 'true' for development mode with mock users (bypasses Zitadel)
VITE_MOCK_AUTH=true

# Mock User ID (1-5) - which mock user to use in development
VITE_MOCK_USER_ID=1

# Zitadel OIDC (Only needed if VITE_MOCK_AUTH=false)
VITE_ZITADEL_DOMAIN=https://zitadel.mysterybubble.eu.org
VITE_ZITADEL_CLIENT_ID=
VITE_ZITADEL_REDIRECT_URI=http://localhost:3000/auth/callback
VITE_ZITADEL_POST_LOGOUT_REDIRECT_URI=http://localhost:3000/
