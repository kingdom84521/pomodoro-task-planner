# Server
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# Database Mode: 'mock' or 'postgres'
DB_MODE=mock

# PostgreSQL (Only needed if DB_MODE=postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pomodoro_planner
DB_USER=postgres
DB_PASSWORD=

# Notion API (Update these when ready to integrate with Notion)
NOTION_API_KEY=
NOTION_DATABASE_ID=

# Security
WEBHOOK_API_KEY=dev_secret_key_123

# Authentication
# Set to 'true' for development mode with mock users (1-5)
MOCK_AUTH=true

# Zitadel OIDC (Only needed if MOCK_AUTH=false)
ZITADEL_DOMAIN=https://zitadel.mysterybubble.eu.org
ZITADEL_AUDIENCE=
